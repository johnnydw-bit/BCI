# Neon PostgreSQL (add via Vercel Neon integration — auto-populates DATABASE_URL)
DATABASE_URL=

# Anthropic API
ANTHROPIC_API_KEY=

# JWT secret — generate with: openssl rand -hex 32
JWT_SECRET=

# Resend email
RESEND_API_KEY=
EMAIL_FROM=noreply@bramleygc.co.uk
MANAGER_EMAIL=manager@bramleygc.co.uk

# App URL
NEXT_PUBLIC_APP_URL=https://bramley-bci.vercel.app

# Cron auth secret — generate with: openssl rand -hex 32
CRON_SECRET=
